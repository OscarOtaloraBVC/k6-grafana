# Dockerfile para imagen con tamaño configurable
ARG SIZE=30
FROM alpine:3.14

RUN dd if=/dev/zero of=/dummy-file bs=1M count=${SIZE}

CMD ["sh", "-c", "echo 'Imagen de prueba de ${SIZE}MB'; sleep infinity"]